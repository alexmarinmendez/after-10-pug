doctype html
html
    include includes/head.pug
    body 
        h2 Create a product
        form(action='/products' method='post')
            label Title:
            input(name='title')
            label Price:
            input(name='price')
            label Thumbnail:
            input(name='thumbnail')
            input(type='submit')
        a.btn.btn-success(href='/products') View products in database
        include includes/foot.pug